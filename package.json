{
  "name": "todo-app-unified",
  "version": "1.0.0",
  "private": true,
  "scripts": {
  "build": "npm run build:frontend && npm run install:backend",
  "build:frontend": "cd frontend && npm install && npm run build",
  "install:backend": "cd backend && npm install",
  "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
  "dev:backend": "cd backend && npm run dev",
  "dev:frontend": "cd frontend && npm run dev",
  "start": "cd backend && npm start",
  "deploy:vercel": "vercel --prod",
  "setup": "npm install && npm run install:backend && cd frontend && npm install",
  "clean": "rimraf node_modules && rimraf backend/node_modules && rimraf frontend/node_modules && rimraf backend/dist && rimraf frontend/dist"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
